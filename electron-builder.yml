appId: com.markdown.reader
productName: Markdown Reader
asar: true

directories:
  output: release

files:
  - out/**
  - package.json

mac:
  category: public.app-category.productivity
  target:
    - target: dmg
      arch:
        - x64

win:
  target:
    - target: nsis
      arch:
        - x64

linux:
  category: Office
  target:
    - target: AppImage
      arch:
        - x64
